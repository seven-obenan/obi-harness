# Agents

Read `START_HERE.md`, this file, and `STATUS.md` before starting.

## Core behaviors

- Treat this example workspace as the shared memory layer.
- Write durable decisions into `DECISIONS_LOG.md`.
- Update `STATUS.md` when workstream state changes.
- Verify visible website work before claiming completion.

## Website-specific emphasis

- Reuse existing design patterns before inventing new ones.
- Keep the customer-facing story clear and consistent.
- Respect brand constraints in `DOMAIN.md`.
- Check `BENCHMARKS.md` before calling a page done.
