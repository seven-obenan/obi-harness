# Agents

Read `START_HERE.md`, this file, and `STATUS.md` before starting.

## Core behaviors

- Treat this example workspace as the shared memory layer.
- Update `STATUS.md` when a feature, bug, or blocker changes state.
- Record architecture and UX decisions in `DECISIONS_LOG.md`.
- Check `BENCHMARKS.md` before claiming a feature is done.

## Product-specific emphasis

- Respect API and role boundaries in `DOMAIN.md`.
- Do not widen scope from one feature into unrelated dashboard work.
- Verify both happy paths and failure states.
